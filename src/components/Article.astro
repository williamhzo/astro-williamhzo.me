---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'article'>;
const { class: className, id, tabindex } = Astro.props;
---

<article
  class:list={['wrapper region prose', className]}
  id={id}
  tabindex={tabindex}
>
  <slot />
</article>
