---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'article'>;
const { class: className, id } = Astro.props;
---

<article class:list={['wrapper region prose', className]} id={id}>
  <slot />
</article>
